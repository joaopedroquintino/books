import 'package:flutter/material.dart';

class TrowelWidget extends StatelessWidget {
  const TrowelWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.abc);
  }
}
